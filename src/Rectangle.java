public class Rectangle {

    public static double area (double longEdge, double shortEdge){
        if (longEdge>=0 && shortEdge >=0) {return longEdge * shortEdge;}

        else {return -1;}
    }
}
